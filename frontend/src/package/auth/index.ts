// Client-safe exports only
// Note: For server-side auth, import from "@/package/auth/server"
// For auth actions (signIn, signOut), import from "@/package/auth/actions"
export type { AuthSession as Session } from "./types"
